/**
 * Supabase client placeholder.
 * Connect with environment variables when the backend is ready.
 */
export function createSupabaseClient() {
  throw new Error("Supabase client is not configured yet.");
}
