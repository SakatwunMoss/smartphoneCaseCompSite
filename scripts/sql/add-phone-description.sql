-- phones テーブルに機種解説文カラムを追加
-- Supabase SQL Editor 等で実行してください。
--
-- 用途:
--   各機種ページ (/phones/[id]) に表示するオリジナル解説文を管理する。
--   300〜500字程度。機種ごとに独自の内容とすること。
--
-- フォーマット (アプリ側 parsePhoneDescription が解釈):
--   - プレーンテキスト: 空行区切りの段落として表示
--   - 構造化 (推奨): ### 見出し でセクション分割
--
-- 例:
--   iPhone 17は薄型化と発熱対策の両立が課題になりやすい世代です。
--
--   ### この機種の特徴
--   6.3インチディスプレイに対し本体は比較的コンパクト。カメラユニットは...
--
--   ### ケース選びのポイント
--   MagSafe対応を優先するなら、マグネット位置が正確なケースを...

-- ============================================================
-- 1. カラム追加
-- ============================================================

ALTER TABLE phones
  ADD COLUMN IF NOT EXISTS description TEXT;

COMMENT ON COLUMN phones.description IS
  '機種ページ用の解説文 (300〜500字)。### 見出しでセクション分割可能。';

-- ============================================================
-- 2. (任意) 既存行の確認用クエリ
-- ============================================================

-- SELECT id, name, maker,
--        CASE WHEN description IS NULL OR trim(description) = ''
--             THEN '未設定' ELSE '設定済' END AS description_status
-- FROM phones
-- ORDER BY released_year DESC, name;

-- ============================================================
-- 3. (任意) 解説文の投入例 — id は環境に合わせて置換
-- ============================================================

-- UPDATE phones
-- SET description = '...'
-- WHERE id = 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx';
